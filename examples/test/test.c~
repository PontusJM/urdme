#include <omp.h>
#include <stdio.h>

int main(){
#pragma omp parallel
  {
  omp_set_nested(1);
  omp_set_dynamic(0);
#pragma omp for
  for(int i = 0; i < 10000; i++){
    //#pragma omp parallel for num_threads(2)
    int j = 2;
    //for(int j = 0; j < 100; j++){
      printf("%d,%d my thread id: %d\n",i,j,omp_get_thread_num());
      //}
  }
  }
}
